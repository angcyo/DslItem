<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application>

        <!--免入侵获取Content-->
        <provider
            android:name=".base.LibInitProvider"
            android:authorities="${applicationId}.init"
            android:exported="false" />
    </application>
</manifest>
